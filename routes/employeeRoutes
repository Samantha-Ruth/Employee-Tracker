// const express = require('express');
// const router = express.Router();
// const db = require('../db/connection');

// Get all employees (id, first name, last name), job title, department, salary, and managers.
// router.get('/employees', (req, res) => {
//     const sql = `SELECT employees.*, parties.name
//     // id, first name, last name, job title, department, salary, managers 
//                   AS party_name 
//                   FROM candidates 
//                   LEFT JOIN parties 
//                   ON candidates.party_id = parties.id`;
                  
//     db.query(sql, (err, rows) => {
//       if (err) {
//         res.status(500).json({ error: err.message });
//         return;
//       }
//       res.json({
//         message: 'success',
//         data: rows
//       });
//     });
//   });



// View all employees by department

// Add an employee (POST)

// Update an employee role 

 // View all employees by manager

 // delete employee


